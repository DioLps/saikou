import { AddBaseUrlToCoverPipe } from './add-base-url-to-cover.pipe';

describe('AddBaseUrlToCoverPipe', () => {
  it('create an instance', () => {
    const pipe = new AddBaseUrlToCoverPipe();
    expect(pipe).toBeTruthy();
  });
});
